#include <stdio.h>
#include <time.h>  //Houses timespec, timeval is in sys/time.h

void main(int argc, char *argv)
{
   if(argc < 2)
   {
      printf("Usage: main %s loop-iterations\n", argv[0]);
      return;
   }

   int iterations = atoi(argv[1]);
   srtuct timespec start, stop;
}
